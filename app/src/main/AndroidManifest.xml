<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shivam.sfl">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:glEsVersion="0x20000"
        android:required="true" />

    <application
        android:name=".SflApplication"
        android:allowBackup="true"
        android:icon="@mipmap/launcher"
        android:roundIcon="@mipmap/launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:supportsRtl="true"
        android:screenOrientation="portrait">

        <!-- The key that shipped in the original APK was extractable from the compiled
             manifest, so it must be treated as compromised. It has been moved out of
             source control into local.properties (MAPS_API_KEY) as a stopgap so it is
             no longer hard-coded here - but you still need to rotate/regenerate it in
             the Google Cloud Console and restrict the new key (package name + SHA-1)
             before using this project for anything real. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.shivam.sfl"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:configChanges="orientation"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CompleteSaveLocation"
            android:label="@string/title_activity_complete_save_location"
            android:screenOrientation="portrait"
            android:configChanges="orientation" />

        <activity
            android:name=".SavedLocationList"
            android:label="Favorite Location"
            android:screenOrientation="portrait"
            android:configChanges="orientation"
            android:windowSoftInputMode="adjustPan" />

        <activity
            android:name=".CollectionsActivity"
            android:label="Collections"
            android:screenOrientation="portrait"
            android:configChanges="orientation" />

        <activity
            android:name=".SettingsActivity"
            android:label="Settings"
            android:screenOrientation="portrait"
            android:configChanges="orientation" />

        <activity android:name=".MapsFragment" />

        <activity
            android:name=".ImportCollectionActivity"
            android:label="Import Collection"
            android:theme="@style/Theme.Material3.DayNight.Dialog"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="sfl" android:host="collection" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".SaveLocationWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/save_location_widget_info" />
        </receiver>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

    </application>

</manifest>
